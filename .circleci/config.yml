# .circleci/config.yml
version: 2.1
jobs:
  beta_android:
    docker:
      - image: cimg/android:2021.08.1-node
    steps:
      - checkout
      - run: yarn deploy_android_beta

workflows:
  deploy-workflow:
    jobs:
      - beta_android

# .circleci/config.yml
version: 2.1
jobs:
  beta_android:
    docker:
      - image: cimg/node:22.2.0
    steps:
      - checkout
      - run: yarn deploy_android_beta

workflows:
  deploy-workflow:
    jobs:
      - beta_android

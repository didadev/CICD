# .circleci/config.yml
version: 2.1
jobs:
  beta_android:
    docker:
      - image: circleci/android:api-30-node
    steps:
      - checkout
      - run: echo "Im a job test within a workflow test"

workflows:
  deploy-workflow:
    jobs:
      - beta_android
      - yarn deploy_android_beta
